// Base path under which GitHub Pages serves this site:
// https://iizzaya.github.io/project-brush/
// Keep in sync with `basePath` in next.config.ts. Needed because vinext does
// not rewrite string paths in `metadata.icons` (and similar raw references).
export const BASE_PATH = '/project-brush';
